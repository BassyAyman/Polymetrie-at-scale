apiVersion: apps/v1
kind: Deployment
metadata:
  name: polymetrie-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: polymetrie-app
  template:
    metadata:
      labels:
        app: polymetrie-app
    spec:
      containers:
      - name: polymetrie-container
        image: igormel/polymetrie:35
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: my-postgresql
              key: password
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: my-redis
              key: redis-password
        ports:
          - containerPort: 8080
        resources:
          limits:
            cpu: 1000m
          requests:
            cpu: 200m
